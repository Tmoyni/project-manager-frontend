const initState = {
    allPosts: [],
    allProjects: []
}

const reducer = (prevState = initState, action) => {
    switch(action.type) {
    default:
        return {prevState}
    }
}

export default reducer